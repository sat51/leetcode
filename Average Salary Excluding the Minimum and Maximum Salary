class Solution {
    public double average(int[] salary) {
        Arrays.sort(salary);
        Double sum = 0d;
        for(int i=1;i<salary.length-1;i++){
            sum+=salary[i];
        }
        Double m = sum/(salary.length-2);
        return m;
    }
}
